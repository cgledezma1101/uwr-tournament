<p><%= t('devise.mailer.salutation_welcome', email: @email) %></p>

<p><%= t('devise.mailer.you_can_confirm') %></p>

<p><%= link_to t('devise.mailer.confirm_account'), confirmation_url(@resource, confirmation_token: @token) %></p>
