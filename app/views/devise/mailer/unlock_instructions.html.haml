%p= t('devise.mailer.salutation_hello', email: @resource.email)

%p= t('devise.mailer.account_was_locked')

%p= t('devise.mailer.click_to_unlock')

%p= link_to t('devise.mailer.unlock_account'), unlock_url(@resource, unlock_token: @token)
