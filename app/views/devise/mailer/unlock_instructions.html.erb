<p><%= t('devise.mailer.salutation_hello', email: @resource.email) %></p>

<p><%= t('devise.mailer.account_was_locked') %></p>

<p><% t('devise.mailer.click_to_unlock') %></p>

<p><%= link_to t('devise.mailer.unlock_account'), unlock_url(@resource, unlock_token: @token) %></p>
