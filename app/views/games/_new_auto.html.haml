-#
	Renders the form needed to create an automatically calculated game in a stage of a tournament. This is meant to be rendered inside a div with the modal-dialog class applied to it

	Requires the following instance variables to be initialized:

	@param [Game] game An instance of a game, which will be used to produce the form
	@param [Array<Team>] teams A collection of the teams that can be matched in this game
#newAutoGame
	.modal-header
		%h2.modal-title= t('game.new')
	.modal-body
		= form_for @game do |f|
			= f.hidden_field :stage_id, value: @game.stage_id
			= f.hidden_field :blue_team_calculation, class: 'js-blue-team-calculation'
			= f.hidden_field :white_team_calculation, class: 'js-white-team-calculation'
			.form-horizontal
				.form-group
					= f.label :starts_at, class: 'col-sm-3 control-label'
					.col-sm-7
						= f.datetime_select :starts_at, class: 'form-control'
				%hr
				.team-details
					.team-label Blue team
					.team-details
						.stage-details
							.stage-label Stage
							.stage-select
								%select.js-blue-team-calculation-stage= options_from_collection_for_select @stages, :id, :name
						.stage-details
							.stage-label Position in stage
							.stage-select
								%input.js-blue-team-calculation-position{ type: 'number', min: 1, value: 1 }
				%hr
				.team-details
					.team-label White team
					.team-details
						.stage-details
							.stage-label Stage
							.stage-select
								%select.js-blue-team-calculation-stage= options_from_collection_for_select @stages, :id, :name
						.stage-details
							.stage-label Position in stage
							.stage-select
								%input.js-blue-team-calculation-position{ type: 'number', min: 1, value: 1 }
				%hr
				.form-group
					= render 'public/modal_form_buttons', form: f
	