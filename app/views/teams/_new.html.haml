-#
  Renders the form required to create a new team for a particular club

  Requires the following instance variables to be initialized

  @param [Team] team The variable for which the form will be built
  @param [Club] club The club to which the team will be added to
#newTeam
  .modal-header
    %h2.modal-title= t('team.new')
  .modal-body
    = form_for @team do |f|
      .form-horizontal
        = f.hidden_field :club_id, value: @club.id
        .form-group
          = f.label :name, class: 'col-sm-4 control-label'
          .col-sm-6
            = f.text_field :name, class: 'form-control'
        .form-group
          .col-sm-offset-4.col-sm-8
            = f.submit t('general.create'), class: 'btn btn-success'
            %a.btn.btn-danger{ data: { dismiss: 'modal' } }
              = t('general.cancel')
