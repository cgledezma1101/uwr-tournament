-#
  Renders navigation buttons on the page's navbar that are specific to the given team

  @param [Team] team Team that's being navigated
= content_for :module_navbar do
  - if can? :edit, team
    %li.dropdown
      = link_to '#', data: { toggle: 'dropdown' }, role: 'button' do
        = t('team.administrate')
        %span.caret
      %ul.dropdown-menu{ role: 'menu' }
        %li
          = link_to t('team.add_player'), '#', class: 'js-show-app-modal', data: { formlink: "#{new_player_path}?team_id=#{team.id}", formid: 'newPlayer'}
        %li
          = link_to t('team.edit'), '#', class: 'js-show-app-modal', data: { formlink: edit_team_path(team), formid: 'newTeam' }
